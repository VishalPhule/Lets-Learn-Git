#include <stdio.h>

int main()
{
    int a, b, div;

    printf("\nEnter two no: ");
    scanf("%d %d", &a, &b);

    div = a / b;

    printf("Mul : %d", div);
    printf("Hello ");

   
    return (0);
}